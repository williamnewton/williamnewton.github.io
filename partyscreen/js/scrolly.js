
$(document).ready(function() {
	
    $(window).on("scroll", function() {
        var fromTop = $("body").scrollTop();
        $('.pic-1').toggleClass("active", (fromTop > 1000));
    });
    $(window).on("scroll", function() {
        var fromTop = $("body").scrollTop();
        $('.pic-2').toggleClass("active", (fromTop > 2000));
    });
    $(window).on("scroll", function() {
        var fromTop = $("body").scrollTop();
        $('.pic-3').toggleClass("active", (fromTop > 3000));
    });
    $(window).on("scroll", function() {
        var fromTop = $("body").scrollTop();
        $('.pic-4').toggleClass("active", (fromTop > 4000));
    });
    $(window).on("scroll", function() {
        var fromTop = $("body").scrollTop();
        $('.pic-5').toggleClass("active", (fromTop > 5000));
    });
    $(window).on("scroll", function() {
        var fromTop = $("body").scrollTop();
        $('.pic-6').toggleClass("active", (fromTop > 6000));
    });
    $(window).on("scroll", function() {
        var fromTop = $("body").scrollTop();
        $('.pic-7').toggleClass("active", (fromTop > 7000));
    });
    $(window).on("scroll", function() {
        var fromTop = $("body").scrollTop();
        $('.pic-8').toggleClass("active", (fromTop > 8000));
    });
    $(window).on("scroll", function() {
        var fromTop = $("body").scrollTop();
        $('.pic-9').toggleClass("active", (fromTop > 9000));
    });
    $(window).on("scroll", function() {
        var fromTop = $("body").scrollTop();
        $('.pic-10').toggleClass("active", (fromTop > 10000));
    });
    $(window).on("scroll", function() {
        var fromTop = $("body").scrollTop();
        $('.pic-11').toggleClass("active", (fromTop > 11000));
    });
    $(window).on("scroll", function() {
        var fromTop = $("body").scrollTop();
        $('.pic-12').toggleClass("active", (fromTop > 12000));
    });
    $(window).on("scroll", function() {
        var fromTop = $("body").scrollTop();
        $('.pic-13').toggleClass("active", (fromTop > 13000));
    });
    $(window).on("scroll", function() {
        var fromTop = $("body").scrollTop();
        $('.pic-14').toggleClass("active", (fromTop > 14000));
    });
    $(window).on("scroll", function() {
        var fromTop = $("body").scrollTop();
        $('.pic-15').toggleClass("active", (fromTop > 15000));
    });
    $(window).on("scroll", function() {
        var fromTop = $("body").scrollTop();
        $('.pic-16').toggleClass("active", (fromTop > 16000));
    });
    $(window).on("scroll", function() {
        var fromTop = $("body").scrollTop();
        $('.pic-17').toggleClass("active", (fromTop > 17000));
    });
    $(window).on("scroll", function() {
        var fromTop = $("body").scrollTop();
        $('.pic-18').toggleClass("active", (fromTop > 18000));
    });
    $(window).on("scroll", function() {
        var fromTop = $("body").scrollTop();
        $('.pic-19').toggleClass("active", (fromTop > 19000));
    });
    $(window).on("scroll", function() {
        var fromTop = $("body").scrollTop();
        $('.pic-20').toggleClass("active", (fromTop > 20000));
    });
    $(window).on("scroll", function() {
        var fromTop = $("body").scrollTop();
        $('.pic-21').toggleClass("active", (fromTop > 21000));
    });
    $(window).on("scroll", function() {
        var fromTop = $("body").scrollTop();
        $('.pic-22').toggleClass("active", (fromTop > 22000));
    });
    $(window).on("scroll", function() {
        var fromTop = $("body").scrollTop();
        $('.pic-23').toggleClass("active", (fromTop > 23000));
    });
    $(window).on("scroll", function() {
        var fromTop = $("body").scrollTop();
        $('.pic-24').toggleClass("active", (fromTop > 24000));
    });
    $(window).on("scroll", function() {
        var fromTop = $("body").scrollTop();
        $('.pic-25').toggleClass("active", (fromTop > 25000));
    });
    $(window).on("scroll", function() {
        var fromTop = $("body").scrollTop();
        $('.pic-26').toggleClass("active", (fromTop > 26000));
    });
    $(window).on("scroll", function() {
        var fromTop = $("body").scrollTop();
        $('.pic-27').toggleClass("active", (fromTop > 27000));
    });
    $(window).on("scroll", function() {
        var fromTop = $("body").scrollTop();
        $('.pic-28').toggleClass("active", (fromTop > 28000));
    });
    $(window).on("scroll", function() {
        var fromTop = $("body").scrollTop();
        $('.pic-29').toggleClass("active", (fromTop > 29000));
    });

});

